---
scope: AI provider boundary and initial Codex App Server adapter
purpose: Keep ProjectOS provider-independent while validating ChatGPT subscription access
altitude: validation-build architecture slice
updated: 2026-07-31T11:31
---

- (constraint) Canonical State, Conversations, Change Proposals, Rationale, and Provenance are ProjectOS-owned and remain provider-independent.
- (decision) Use Ports and Adapters with a narrow ProjectOS AI capability port; provider adapters advertise capabilities and normalize events, errors, usage, and session lifecycle.
- (decision) Use Codex App Server as the first adapter over a ProjectOS-owned stdio child process; validation may require an installed compatible Codex CLI.
- (decision) Codex owns managed ChatGPT browser authentication and tokens; ProjectOS observes account, plan, allowance, and auth events without reading credentials.
- (decision) ProjectOS Conversation IDs are canonical; provider session IDs are replaceable bindings whose persistence, resume, and deletion are adapter-owned.
- (decision) Generate protocol schemas from the validated Codex version, validate ProjectOS-owned structured output, and keep the adapter behind contract tests including a fake provider.
- (question) Commercial redistribution remains open: require installed Codex versus bundle and update a pinned runtime; resolve after the validation spike and before Mac App Store work.
- (decision) Launch Codex with a ProjectOS-scoped CODEX_HOME, force ChatGPT login, require macOS keyring credential storage, and fail setup closed rather than falling back to plaintext auth cache.
- (change) Reviewer fixes tightened exact-build compatibility, restricted execution, truthful error classification, capability freshness, crash-safe cleanup, export/restore isolation, event ordering, idempotent proposal persistence, and dedicated runtime-profile ownership.
- (event) Deterministic architecture lint passed with zero findings after reviewer fixes.
- (event) Independent rubric, current-platform, and adversarial re-review passed with no remaining critical or high findings.
- (event) spine finalized
