---
topic: Codex agent conversations in ProjectOS
updated: 2026-09-17T07:34
---

- (direction) 2026-09-17 user requests a spec/plan for the proven Codex feature; this turn changes documentation only, not application code or runtime configuration.
- (direction) Native ProjectOS chat and research with approved tools; project changes remain proposals. Codex uses its built-in tools, never SearXNG; SearXNG is local-LLM only. Context preview must distinguish retained agent history.
- (decision) Slug codex-agent-conversations; derive a kernel plus scope-and-evidence, experience, implementation-plan and acceptance companions. Rich conversation plus experiment evidence permits direct extraction.
- (note) Baseline 4ecf4b2 on feat/codex-spike is clean. Experiment REPORT.md proves synthetic native search, cancellation, proposal validation and resume, not production isolation, packaging or personal usefulness. project-context.md is absent.
- (decision) Latest user direction supersedes only the old MVP exclusion of Codex/subscription authentication and provider-owned research tools; retain all project integrity, local ownership and explicit action constraints. Do not rewrite historical plans.
- (assumption) A1: Personal-use macOS release with guided setup; optional distribution question is pending. Do not claim App Store feasibility or require public distribution for this release.
- (assumption) A2: Implement Codex first using the proven ACP adapter boundary; support later agents through capability-aware contracts and a fake second adapter, without adding another real provider now.
- (assumption) A3: Explicitly enabling Research approves native search/page reads for subsequent turns until disabled; other tool families are disabled in this release. No per-search confirmation unless Codex actually requests one.
- (assumption) A4: A conversation is bound to a backend once it sends its first turn. Changing backend creates an explicit continuation conversation with a selectable transfer packet; changing Codex model is an explicit next-turn operation.
- (capability) CAP-1 intent: connect an owned Codex account and select an available model; success: setup/recovery state is clear, model is visible, and no credential or billing fallback occurs.
- (capability) CAP-2 intent: chat inside normal project conversations; success: streaming, stop, draft retention and failure recovery work without a terminal or accepted-state mutation.
- (capability) CAP-3 intent: research using the selected provider tool path; success: Codex native search/page activity and citations are shown, local Ollama alone uses SearXNG, and unknown/disallowed actions cannot run.
- (capability) CAP-4 intent: inspect and limit context disclosure; success: new context, previously disclosed history and freshness are distinguishable and Start fresh starts a new session without deleting prior transcript.
- (capability) CAP-5 intent: request reviewable project updates; success: production schema/exact-evidence/revision checks gate pending proposals and only existing application acceptance commands mutate knowledge.
- (capability) CAP-6 intent: resume and recover conversations; success: sessions and activity survive relaunch without duplicated or silently replayed turns and uncertain remote state is explicit.
- (capability) CAP-7 intent: own/export/delete agent conversations; success: archives retain content without credentials/live session handles, restored copies cannot attach originals, and deletion fences late events with honest external-retention disclosure.
- (capability) CAP-8 intent: continue using local/direct providers while permitting future agent providers; success: Ollama/OpenRouter workflows remain valid and a fake agent conforms without UI or domain branching on Codex JSON.
- (constraint) Keep main app sandbox enabled. A helper may run outside it only with an explicit launch/distribution design; model tools must not access production DB, arbitrary home files, shell, MCP/connectors, skills/hooks/plugins or computer/browser-control paths.
- (constraint) Runtime qualification precedes real project context: empty MCP list, read-only labels and model refusals are not enforcement. Verify actual tool inventory, sandbox roots and configuration inheritance; if unsupported remain unavailable for real context, not silently downgrade.
- (constraint) Credentials stay under Codex-managed auth; ProjectOS never parses/copies tokens. No API keys, hosted proxy, invisible provider/model changes, prompt resend or automatic billing fallback.
- (decision) Persist conversation binding/session generations, immutable turn context and provenance, terminal status, durable agent activity, permissions decisions and replay mapping in app store. One active turn per project, one owner per session; explicit retry has a new attempt ID.
- (decision) Resume reconciles stable remote items where supported; ambiguous replay is marked unreconciled and cannot silently continue. No wholesale replay drop as production success; user may explicitly start fresh using visible selected context.
- (decision) Proposal generation uses an isolated short-lived generation session with tools disabled and only the reviewed evidence packet, not hidden chat history. All existing production artifact kinds remain supported; experiment question-only restriction is not product scope.
- (decision) Native citations remain links, not retained page evidence. Existing user-pasted sources can support proposals; automatic capture of cited pages and import of third-party Codex history are non-goals.
- (decision) Deliver in order: P0 runtime decision and qualification gates; P1 session contracts/persistence; P2 guided runtime setup; P3 chat/context/research; P4 proposals; P5 recovery/ownership; P6 integrated live qualification. Implementation authorization is separate from this spec.
- (question) Q1: Confirm first-release distribution target; personal-use guided setup is a provisional default. Q2: P0 must select an enforceable runtime/helper design and prove auth/config isolation; evidence can require revising ACP transport, but never silently changing provider or account.
- (note) Official Codex App Server docs fetched 2026-09-17 describe managed ChatGPT authentication and restricted sandbox read roots; these are candidate underlying-runtime capabilities, not proof that pinned ACP exposes them.
- (note) Wrapper-only content omitted: workflow menus and prior command chronology. Historical experiment evidence remains immutable; no feature implementation or live subscription runs are authorized by this specification-only turn.
- (direction) User confirmed personal-use macOS app with guided Codex setup. This resolves Q1 and supersedes provisional A1; public distribution and App Store remain out of scope.
- (decision) UX defaults: ordinary conversations start research off, research conversations disclose their enabled default; existing conversations bind a provider only on the next explicit send, with no invented historical attribution. Start fresh keeps transcript with a generation divider.
- (decision) Guided setup may require explicit installation and Codex-managed browser/device login, but routine use must not require a terminal or recurring manual pairing. Codex next-action generation is deferred, with no silent direct-provider substitution.
- (decision) Architecture defaults: keep direct AIProvider separate from AgentSessionProvider; use an app-owned actor/service and durable per-turn event journal, exact sent-context manifests, generation fences, account/runtime identity and machine-local provider handles. Restored archives are detached.
- (decision) P0 evaluates managed helper startup from the signed sandboxed app and actual model-tool restrictions separately from internal runtime/auth storage. Unsupported capabilities fail closed, with a recorded transport ADR rather than misleading readiness.
- (decision) Persistence defaults: next SQLite/archive versions are additive; migrate v3 records without claiming a prior provider, use project+conversation ownership checks, atomically persist journal and transcript, redact sensitive payloads and preserve corrupt files.
- (decision) Verification will include clean setup/login, research-off enforcement, cross-project/account isolation, permission expiry, cancel/timeout races, durable replay deduplication, fresh context, proposal provenance, export/restore and delete fencing; deterministic, live and personal-use evidence stay separate.
- (decision) P0 may use a dedicated runtime-owned profile with a fresh Codex-managed login if shared authentication cannot be isolated safely; never copy credentials. Exports omit account identifiers as well as attachable session handles.
- (event) Coherence validation PASS: eight stable unique capabilities each have intent/success; constraints exclude concrete alternatives; non-goals and demonstrable success are explicit; implementation defaults A2-A4 are labeled. Q1 is resolved by the user; Q2 remains an engineering qualification gate, not a claim of shipping readiness.
- (event) Preservation validation PASS: native chat, approved tools, Codex built-in research versus local-only SearXNG, truthful retained context, pending-only changes, future provider extension and confirmed personal-use setup all map to capabilities and acceptance. Experiment isolation/replay/packaging gaps and existing ownership/proposal invariants are retained. Wrapper-only omissions are recorded in scope-and-evidence.md.
- (event) Structural validation PASS: required five-field kernel, eight unique capability pairs, all local links and 24 Given/When/Then acceptance scenarios checked. Specification-only handoff complete; no application changes, runtime operations or live provider calls performed.
