---
topic: ProjectOS personal-use native macOS MVP with Ollama and OpenRouter
updated: 2026-09-10T15:38
---

- (direction) User requests a spec for another agent to start the MVP, explicitly prohibits implementation in this turn, and confirms prior spike changes were committed; current working tree was verified clean.
- (direction) User knows the continuity problem from experience and wants a working app to test usefulness personally; integrated conversation is required because cross-app context handoff was rejected as excessive friction.
- (direction) Current choice is local models plus OpenRouter; Apple PCC may be chosen later and is not a prerequisite, deliverable, or entitlement requirement for this build.
- (decision) Create spec-projectos inherited from the ProjectOS source identity. This is a bounded owner-test vertical slice spanning foundation, conversation, re-entry and ownership, not completion of the existing four-provider MVP or all epics.
- (assumption) Use Ollama as the first local runtime plus OpenRouter, as proposed in the preceding scoped MVP and referenced by the user's request for this MVP; LM Studio, native MLX and Apple inference are deferred for this slice.
- (assumption) Use a SwiftUI/AppKit native macOS 26 application with Swift 6.2, SQLite transactional local persistence and a Foundation-only domain package. These are implementation defaults, not previously validated hardware or model compatibility claims.
- (assumption) Ship English interface copy in this slice while preserving Unicode and testing English/Dutch project content; full UI localization, commercial visual presets, graph navigation and cross-device surfaces remain deferred.
- (capability) CAP-1: create, rename, reopen and manage local projects and labelled pasted source material without AI or an account; successful restart preserves source text and separate project identities.
- (capability) CAP-2: configure Ollama or Keychain-backed OpenRouter and explicitly choose a model; verify availability without claiming semantic qualification from a health check; record actual combinations used in live validation.
- (capability) CAP-3: converse within a project using an inspectable selected context snapshot, streaming response and stop; retain drafts and incomplete transcripts on errors, never silently truncate, retry, reroute or transmit on navigation.
- (capability) CAP-4: explicitly generate typed Topic, Research, Decision, Open Question and Task proposals from selected complete conversation turns and source text; validate the entire structured result and exact provenance before persisting pending proposals.
- (capability) CAP-5: review, edit/accept or reject proposals; direct user edits, decision replacement, explicit relations, artifact removal and undo append history atomically; stale or duplicate acceptance never overwrites current state.
- (capability) CAP-6: reopen to offline current governing decisions, open work, recent research and changes since the last visit; generate an optional explained next action from accepted state with revision-bound freshness and source links.
- (capability) CAP-7: export human-readable state plus versioned JSON, verify checksums and restore as a separate project with equivalent graph/history/provenance; confirm permanent local project deletion and separate credential removal.
- (capability) CAP-8: record local return outcomes, correction/review effort and final project outcomes; deliver a runnable app, reproducible build instructions, meaningful deterministic checks and separately labelled live-provider evidence.
- (constraint) Application owns canonical project and conversation identifiers, accepted versions, mutation transactions and evidence. Provider adapters generate only: no tool execution, filesystem authority, web search, connector access, accounts or project-content backend.
- (constraint) Provider-independent port has explicit chat, proposal and recommendation jobs, capability states, ordered events, bounded context/output/timeouts and cancellation. One active AI job per project; in-flight snapshots retain originating project, model and revision.
- (constraint) Ollama uses fixed loopback IP endpoint and verified locally executing models; reject remote targets, off-loopback redirects and Ollama cloud-backed models rather than calling a loopback proxy local inference. User owns runtime install/start/download lifecycle.
- (constraint) OpenRouter uses the user's Keychain key and HTTPS, an explicitly selected stable model and routing configuration, no app retries or model fallback; require supported structured parameters and disable provider fallback. Show external cost disclosure and capture returned usage honestly.
- (decision) Use separate explicit Send and Suggest project updates actions, each with an inline inspectable context preview. No mandatory modal per turn, no hidden second paid inference call, no auto-acceptance, no obligation to review every turn, and pending proposals never block conversation.
- (decision) Context includes project description, chosen accepted artifact versions, chosen source excerpts and a visible conversation range. Before submission freeze IDs/revisions/content; sources and quotes are untrusted data, oversized context requires visible narrowing, source text is never silently edited.
- (decision) Proposal wire contract has schemaVersion and proposals with operation, kind, target ID/revision, title, content, rationale, evidence references and typed relationships. New proposals use temporary IDs. Application validates all cross-references, subjects and dependencies; dependent sets are accepted atomically.
- (constraint) Evidence references resolve to an exact source/message version and exact quoted substring without markup normalization or invented spans. Missing context produces empty proposals or explicit uncertainty. Valid quotations do not prove semantic correctness; flag assistant-derived evidence and unsupported claims for human review.
- (constraint) Native project library, Overview, Conversation with proposal rail, Knowledge lists/inspector and Settings are required. Native text, focus and keyboard support, status text beyond color, light/dark appearance, recoverable drafts and offline browsing are retained; adopt visual references only within scoped surfaces.
- (decision) Persist immutable source/message versions, typed artifacts and relations, accepted-change history, proposal lifecycle, context snapshots, jobs, saved recommendations and return outcomes in transactions; use one store writer and do not claim saved until commit.
- (constraint) Failure must preserve last coherent state; stale dependencies require explicit rebase and renewed review, supersession preserves exactly one current decision per resolved subject, undo appends a compensating transaction, and deletion cancels jobs and rejects late completions.
- (decision) Implementation tasks will name app/package paths in dependency order: native shell and store, sources/context, real adapters, integrated chat/proposals, mutation/review, offline overview/recommendation, export/delete/outcomes, tests and packaging. Only this spec folder is created in this turn.
- (decision) Acceptance companion will include Given/When/Then checks for happy paths, transactional rollback, invalid and fabricated evidence, cancellation/stream fragmentation, project isolation, unsupported context/provider combinations, credential handling, restore/deletion and live re-entry walkthrough.
- (constraint) No claim of live provider readiness from fakes or old screening scores. Deliver independent live Ollama and OpenRouter loop results when user runtime/key/budget are available; otherwise deliver implementation with explicit unverified provider status and exact remaining steps.
- (question) Runtime inputs before live tests: Wouter selects an installed local model and hardware, enters OpenRouter credentials and chooses a model/routing target and test spending ceiling. These do not block source implementation and must never be invented or extracted from unrelated files.
- (decision) Preserve PRD FR1-18 core outcomes in the slice, qualify FR14/FR18 breadth to two providers, carry integrity NFR1-9/11/13, retain NFR10 performance benchmark, defer NFR12 full bilingual UI matrix explicitly; inherited product metrics remain personal-use observations, not implementation or market-proof claims.
- (decision) Author source mapping identifies older full-MVP provider breadth, UX file intake versus PRD paste-only limit, full visual surfaces, and exploratory iOS/listing additions as excluded from this slice; do not modify governing docs, old spike results or sprint statuses, and never revive rejected Codex work.
- (note) Official API documentation checked 2026-09-10: docs.ollama.com/api/chat and /capabilities/structured-outputs; openrouter.ai/docs/guides/features/structured-outputs and /guides/routing/provider-selection. JSON schema enforcement differs by provider and always needs application validation.
- (event) Rich conversational and repository input is sufficient for direct distillation; implementation defaults are labelled assumptions and unresolved runtime/model/budget choices remain explicit prerequisites for live evidence.
- (constraint) Coherence refinement: a structured job whose source project revision changed before completion cannot publish pending proposals or a fresh next action; preserve the completed chat separately and require explicit regeneration. Acceptance also checks current target and dependency revisions.
- (assumption) A4: Separate Send from Suggest project updates with inline context disclosure, rather than auto-extracting after each turn. This is the first implementation interaction default to limit hidden calls and allow Wouter to assess review friction.
- (decision) For initial packaging enable compatible macOS App Sandbox entitlements for outbound requests and user-selected export/restore locations; local build/run cannot depend on paid membership, broad filesystem access or installing another runtime from the app.
- (note) Implementation handoff adds exact native app/package paths, stable record/proposal fields, Unicode substring evidence validation, 250000-character paste intake, one job per project, atomic restore as a new identity, and explicit success versus stale/cancelled output. Details are derived in implementation.md and acceptance.md.
- (event) Self-validation pass 1 COHERENCE: PASS after refining stale structured-job completion and naming the separate proposal action as A4. Eight unique capabilities each contain intent/success; constraints/non-goals are explicit; 23 Given/When/Then cases cover all capabilities; A1-A4 defaults and Q1-Q2 runtime/personal-use inputs are visible. Frontmatter, three companion paths, local links and new-file whitespace checks passed.
- (event) Self-validation pass 2 PRESERVATION: PASS for the scoped source claims, with explicit mapping of PRD FR1-18/NFRs, architecture invariants, native UX, current integrated local/OpenRouter direction, deferred full-provider/mobile/visual work, and unchanged historical Codex rejection. Wrapper-only content and narrower-slice authority are recorded in scope-and-sources.md.
- (event) Spec finalized for implementation handoff: SPEC.md plus scope-and-sources.md, implementation.md and acceptance.md. Only _bmad-output/specs/spec-projectos was created; Git status shows no application or prior-artifact changes. No app implementation, model invocation, provider credential access, paid inference, App Store action, or test of product behavior was performed.
